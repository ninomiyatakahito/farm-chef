<h1>Posts#index</h1>

<p>Find me in app/views/posts/index.html.erb</p>

<% if user_signed_in? %>
  <li><%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: "logout"%></li>
<% else %>
  <li><%= link_to 'ログイン', new_user_session_path %></li>
  <li><%= link_to '新規登録', new_user_registration_path %></li>
<% end %>

  <li><%= link_to '新規POST', new_post_path %></li>
  <li><%= link_to 'ユーザーの一覧', users_path %></li>

<%# ログインしているときは以下を表示する %>
      <% if user_signed_in? %>
        <div class="greeting"> 
           こんにちは、
           <%= link_to "#{current_user.last_name}さん", "/users/#{current_user.id}", class: :hoge%>
         </div> 
      <% else %>
<%# // ログインしているときは上記を表示する %>
      <% end %>

          <%#= link_to image_tag(post.image, class: :card__img ), post_path(post.id), method: :get %>
      <%#= link_to "by#{post.user.last_name}", user_path(post.user.id), class: :card__user %>
       <% @posts.each do |post| %>
        <%= link_to image_tag(post.image), post_path(post.id) %>
        <%= link_to post.post_text, post_path(post.id) %>
        <%= link_to "by#{post.user.last_name}", user_path(post.user.id) %>
      <% end %>
